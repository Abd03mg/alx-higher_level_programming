#!/usr/bin/python3
""" Find a peak"""


def find_peak(lot):
    ''' function that finds a peak.
        Args:
            lots: List.Of.inTegers
    '''

    return None if len(lot) == 0 else max(lot)
